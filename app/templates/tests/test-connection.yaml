apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "external-dns-power.fullname" . }}-test-connection"
  labels:
    {{- include "external-dns-power.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "external-dns-power.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
