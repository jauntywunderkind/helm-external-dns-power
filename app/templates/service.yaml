apiVersion: v1
kind: Service
metadata:
  name: {{ include "external-dns-power.fullname" . }}
  labels:
    {{- include "external-dns-power.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "external-dns-power.selectorLabels" . | nindent 4 }}
