apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "external-dns-power-persist.fullname" . }}-test-connection"
  labels:
    {{- include "external-dns-power-persist.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "external-dns-power-persist.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
