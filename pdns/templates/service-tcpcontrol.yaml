apiVersion: v1
kind: Service
metadata:
  name: {{ include "extpdns.fullname" . }}-pdns-tcpcontrol
  labels:
    {{- include "extpdns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.pdns.service.tcpcontrol.type | default  .Values.service.type }}
  ports:
    - name: tcpcontrol
    {{- with .Values.pdns.service.tcpcontrol }}
    {{- range $key, $value := . }}
      {{ $key }}: {{ $value }}
    {{- end }}
    {{- if not (hasKey . "targetPort") }}
      targetPort: {{ .port }}
    {{- end }}
    {{- end }}
  selector:
    {{- include "extpdns.selectorLabels" . | nindent 4 }}
