apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "extpdns.fullname" . }}-config
  labels:
    {{- include "extpdns.labels" . | nindent 4 }}
data:
  server-id: {{ include "extpdns.fullname" . }}
  config-name: {{ include "extpdns.fullname" . }}
  gpgsql-host: {{ include "extpdns-persist.fullname" . }}
  {{- range $key, $value := .Values.pdns.config }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
